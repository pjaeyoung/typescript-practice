// function sum(a, b) {
//   return a + b;
// }

// sum(10, "20"); // 예상값 : 30 , 결과값: 1020

/**
 *
 * @param {number} a 첫 번째 숫자
 * @param {number} b 두 번째 숫자
 * @returns {number} 덧셈 결과
 */

//@ts-check   -> 타입스크립트처럼 에러를 발생시킴
function sum(a, b) {
  return a + b;
}

sum(10, "20"); // 커서를 가까이 하면 js docs 내용이 나타남
